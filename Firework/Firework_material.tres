[gd_resource type="ShaderMaterial" load_steps=4 format=3 uid="uid://bp6ngh4try4mx"]

[sub_resource type="Shader" id="Shader_t0r85"]
code = "// NOTE: Shader automatically converted from Godot Engine 4.5.1.stable's StandardMaterial3D.

shader_type spatial;
render_mode blend_mix, depth_draw_opaque, cull_back, diffuse_burley, specular_schlick_ggx, unshaded, depth_prepass_alpha;

uniform vec4 albedo : source_color;
uniform sampler2D texture_albedo : source_color, filter_linear_mipmap, repeat_enable;

uniform vec3 uv1_scale;
uniform vec3 uv1_offset;

uniform vec3 outline_color: source_color = vec3(1.0,0.2,0.0);
uniform sampler2D fade;

void vertex() {
	UV = UV * uv1_scale.xy + uv1_offset.xy;
	vec3 direction = - VERTEX + CAMERA_POSITION_WORLD;
	//vec3 direction = CAMERA_DIRECTION_WORLD;
	float angle = acos(dot(direction, NORMAL) / (length(direction) * length(NORMAL)));
	if(PI - angle * 2.0 < 0.25) angle = PI / 2.0;
	COLOR = mix(mix(vec4(1.0,1.0,1.0,1.0), vec4(outline_color, .0), texture(fade, vec2(sin(angle), 1)).x), vec4(.0,.0,.0,.0), step(0.49*PI, angle));
}

void fragment() {
	vec2 base_uv = UV;

	vec4 albedo_tex = texture(texture_albedo, base_uv);

	// Vertex Color Use as Albedo: Enabled
	albedo_tex *= COLOR;

	ALBEDO = albedo.rgb * albedo_tex.rgb;
	ALPHA *= albedo.a * albedo_tex.a;
}
"

[sub_resource type="Curve" id="Curve_ysxqe"]
_data = [Vector2(0, 0), 0.0, 0.0439927, 0, 0, Vector2(0.550279, 0.0912981), 0.379377, 0.379377, 0, 0, Vector2(0.99162, 1), 3.45786, 0.0, 0, 0]
point_count = 3

[sub_resource type="CurveTexture" id="CurveTexture_jxvxo"]
curve = SubResource("Curve_ysxqe")

[resource]
render_priority = 0
shader = SubResource("Shader_t0r85")
shader_parameter/albedo = Color(1, 1, 1, 1)
shader_parameter/uv1_scale = Vector3(1, 1, 1)
shader_parameter/uv1_offset = Vector3(0, 0, 0)
shader_parameter/outline_color = Color(0.75, 0.3625, 0, 1)
shader_parameter/fade = SubResource("CurveTexture_jxvxo")
